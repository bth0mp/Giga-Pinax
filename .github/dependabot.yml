# The workflows pin each action to a full commit SHA. Dependabot proposes each new release as a pull request, so an
# action moves only when a person has reviewed the change and merged it.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  # The web-ext the workflows lint and sign with, pinned with its whole tree in tools/web-ext/package-lock.json.
  - package-ecosystem: npm
    directory: /tools/web-ext
    schedule:
      interval: weekly
  # The TypeScript CI checks the JSDoc types with, pinned in tools/typescript/package-lock.json.
  - package-ecosystem: npm
    directory: /tools/typescript
    schedule:
      interval: weekly
  # The Playwright the optional Browser checks run, pinned in e2e/package-lock.json.
  - package-ecosystem: npm
    directory: /e2e
    schedule:
      interval: weekly
