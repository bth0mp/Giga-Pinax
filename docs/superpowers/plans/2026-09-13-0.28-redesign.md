# Giga Pinax0.28 Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement and review the independent tasks below.

**Goal:** Implement all eight approved product improvements as a cohesive, tested extension release.

**Architecture:** Retain vanilla WebExtension modules and the existing durable command writer. Reuse shared design, calculator, source and navigation modules across the research document and coin workspace; give settings one home.

**Tech Stack:** JavaScript ES modules, HTML/CSS, Node tests, Python deterministic packaging, native Brave/Firefox validation.

**Spec:** ../specs/2026-09-13-0.28-redesign.md

## Global constraints

- Base75d70958f35a02804ce3d06fe5ff31e06fc3ca99; branch codex/0.28-redesign in C:/Users/bth0m/CodeWorktrees/giga-pinax-0.27.
- Sol implementation, Astra design and final review. No child agents or concurrent builders. Root alone stages/commits/integrates/publishes after review.
- Release0.28.0, schema1, unchanged extension IDs and research hosts. Preserve existing record and pricing boundaries and all previous audit fixes.
- Follow the exact shared interfaces in the spec. Coordinate changes rather than editing another owner's files.
- Behavioral tests must cover actual new behavior; do not test only markup strings or duplicate the implementation. Visual CSS changes are verified natively.

## Task1: Shared platform and reusable tools

**Owner:** Sol platform. **Files:** new design-tokens.css, bid-tools.js/css, navigation.js, source-menu.js, settings.html/js/css; core/money.js, core/records.js, store.js, background.js, manifests, scripts/build.py, package tests; associated new/owned tests. Own version/install/release docs after the implementation is ready.

**Produces:** exact exports listed in spec; optional housePremiumPresets and lot.notes; packaged sidepanel/sidebar settings.

- [ ] Add failing money tests for maximum affordable hammer, zero/unknown BP, fractional rounding, locale boundaries and unsafe integer input. The core export calculateMaximumHammer(budget,buyerPremiumBps) returns {ok,value:{hammer,premium,hammerPlusPremium}} or the established error shape. For every accepted result, assert calculatePremium(hammer,bps).total<=budget and the next penny exceeds it.
- [ ] Implement inverse arithmetic using checked integer/BigInt calculations and the existing forward rounding. Retain calculatePremium behavior.
- [ ] Add preferences/lot writer and backup regression tests: old schema1 loads, presets/notes survive save/export/import, invalid duplicates/limits reject atomically, omitted new fields preserve prior values, stale revisions fail.
- [ ] Implement optional fields through current validators/draft whitelists and preferences.save; no second storage backend.
- [ ] Build the shared calculator with total/budget modes, preset selection and explicit Use in bid callback; never mutate an active bid. Save presets through the same revision-checked preferences path and show errors visibly.
- [ ] Build tokens, Sources and Settings. Reuse backup preview/confirmation logic with a collector-readable counts summary, stale-preview guard and actionable import validation. Theme remains compatible with the existing key. Updates uses current stable asset links.
- [ ] Implement navigation tests for Chrome native open, Firefox synchronous gesture call, unsupported/rejected fallback and complete failure. Implement native research document declarations and the shared navigation adapter without replacing the toolbar popup.
- [ ] Update both manifests to0.28.0, add all new modules/CSS/HTML to the build allowlist, and adjust manifest/package expectations. Announce the final new-file list to root.
- [ ] Run focused tests and write .superpowers/sdd/2026-09-13-0.28-redesign/platform-report.md including changed files, commands/results and limitations. Do not build until root coordinates it.

## Task2: Research popup and persistent research document

**Owner:** Sol research. **Files:** popup.html, popup.css, popup.js, companion-popup.css, companion-popup.js; associated popup/companion/preferences-selection-facing tests only. Do not edit shared modules or records.

**Consumes:** shared tokens; mountBidCalculator, mountSourcesMenu, openResearchPanel/openSettings/openWorkspace. **Produces:** quick popup and popup.html?panel=1 responsive document, preserved existing parser/pricing behavior.

- [ ] Map every existing DOM ID/controller assumption before moving markup. Keep reference submission, cancellation tickets and existing choice behavior correct.
- [ ] Build a quiet header with panel, Sources and Settings actions. Remove inline Updates and the duplicate Pro area. Keep monogram, three existing tools accessible and one primary search.
- [ ] Move guided reference fields into native Refine reference disclosure. Ensure relevant ambiguity/error paths reveal it without clearing quick input. Test choice/refine state behavior rather than CSS.
- [ ] Put result and pricing evidence immediately after search. Show specific sample/period/source/currency/basis information, preserve inspectable sales and copy summary. Avoid fake probability/confidence labels; keep empty/limited/error/sign-in states legible.
- [ ] Foreground current-page capture with an explicit source summary and review actions. Pin tab identity for async capture, expose errors and preserve corrections. No automatic page extraction on unrelated tab events.
- [ ] Replace standalone calculator controls with the shared widget; remove obsolete listeners safely while preserving exported pure functions/tests where still useful.
- [ ] Make panel mode fill its viewport, preserve the selected research document while tabs change, and distinguish it from a windowed lookup listener so it cannot steal pop-out messages. Use navigation adapter for user actions.
- [ ] Add keyboard focus/search/refinement behaviour and visible error/save feedback, preserve user's currency and recent terms, minimise routine status noise. Keep successful one-action current-source/research/watchlist paths.
- [ ] Run focused regressions, inspect400x600 and320px layouts where possible, and write research-report.md in the task ledger directory. Do not build or launch duplicate QA browsers.

## Task3: Coin-focused workspace

**Owner:** Sol workspace. **Files:** workspace.html/css/js; workspace-specific tests. Do not edit core models or shared modules.

**Consumes:** tokens, shared calculator/Settings/Sources; optional lot.notes; all existing commands. **Produces:** compact list + selected-lot detail, related bid/reminder/outcome actions with stable identity.

- [ ] Preserve existing tested command constructors and editor-session/version semantics. Add failing tests for selected-record routing and context preservation before restructuring controller behavior.
- [ ] Render compact selectable rows with reference/title, event/time, planned/active bid and clear status; provide filtering/search and deliberate Add coin. Unselected state invites selecting a coin without displaying empty editors.
- [ ] Group Details/Notes, Bid, Reminders and Outcome around selectedLotId; maintain a single selection across relevant forms. Existing legacy links/routes continue to reach sensible views. Save replies must never retarget another selected lot or another new draft.
- [ ] Add optional notes to lot drafts. Show attached event/reminder state and a clear edit/create event path; inline shared calculator updates only draft bid fields on explicit Use in bid.
- [ ] Keep alternative membership/order controls near list rows. Keep event list and recorded bid exposure available. Move backup controls/controller to shared Settings and collection history alongside outcomes.
- [ ] Replace internal language, raw status strings and JSON previews with collector-facing labels. Save confirmations/errors are visible near detail actions. Implement one safe revision-checked Undo for an existing lot details edit, retaining captured previous record/basis and refusing stale undo; do not claim external bid cancellation.
- [ ] At narrow widths show list or detail with Back; selecting a row and returning preserves filter/position. Ensure keyboard selection and no focus traps; don't lose inputs silently when switching contexts.
- [ ] Run workspace regressions including delayed response+New draft, existing A-to-B switch and undo conflict. Write workspace-report.md; no build or duplicate browser launch.

## Task4: Integration, review, verification and delivery

**Owner:** Root coordinates; Astra reviews; Sol owners fix their changes. **Files:** all review artifacts, final docs and package metadata.

- [ ] Reconcile shared interfaces and all eight spec items; perform Astra design review on the integrated screens. Resolve concrete defects through the responsible Sol owner.
- [ ] Build once after all assets are ready. Run node --test tests/*.test.mjs, bundled Python tests and web-ext lint. Validate allowlists, IDs, permissions and deterministic artifacts.
- [ ] Native QA of the packaged extension in an isolated Brave profile: existing0.27.2 records, search/refinement/liveANS/logged-out prices, Sources/Settings, total/maxbid/presets, selectedlot/saveraces/undo, backups, popout/nativepanel/fallback and errors, themes/keyboard/narrowlayout. Check native Firefox temporary install and sidebar as feasible; disclose remaining actual-browser limits.
- [ ] Update README, INSTALL, release0.28.0 notes and a concise design/verification record with actual results, not guessed counts. Every release includes brief install/update instructions.
- [ ] After review passes, commit known files, fetch and fast-forward the installed Z: main checkout, push, rebuild Z:dist, compare packaged content. Publish draft5assets, verify hashes/downloads, publish latest and check anonymous stable links.
- [ ] Final answer reports improvements, tests, installed-folder Reload instruction, release link and material limitations. No end-user Python step.
