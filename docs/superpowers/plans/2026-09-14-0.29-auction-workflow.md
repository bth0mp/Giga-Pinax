# Giga Pinax 0.29 implementation plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development for the bounded tasks below.

**Goal:** Complete every approved review fix and workflow improvement, verify an upgrade and deliver release 0.29.0.

**Architecture:** Existing vanilla WebExtension, schema 1, durable command writer and shared calculator. Optional metadata and pure helpers extend current flows.

**Tech stack:** JavaScript modules, HTML/CSS, Node behavioral tests, Python deterministic packaging, native Brave and Firefox.

**Spec:** ../specs/2026-09-14-0.29-auction-workflow.md

Base 519f859; branch codex/0.29-auction-workflow in the existing isolated C:/Users/bth0m/CodeWorktrees/giga-pinax-0.27 worktree. Root coordinates, reviews, builds and publishes; Sol agents implement; no nested agents. No concurrent builders or changes in installed Z until verified integration.

1. Core owner: extension/core/money.js, extension/core/records.js, extension/core/lot-context.js, extension/store.js and corresponding money/records/store/backup/lot-context tests. Add frozen money, metadata, duplicate writer contracts; retain limits and replay/concurrency semantics. Write meaningful regression tests, run focused tests, report exact results.
2. Research/calculator owner: extension/current-lot.js, extension/companion-popup.js, extension/popup.js, extension/popup.html, extension/popup.css, extension/companion-popup.css, extension/prices.js, extension/bid-tools.js, extension/bid-tools.css and corresponding tests. Implement capture continuity and duplicate-save guard, session curation, calculator fees/grid and live presets, reference query handoff. Coordinate with core APIs; do not change workspace files.
3. Workspace owner: extension/workspace.js, extension/workspace.html, extension/workspace.css and workspace tests (new dedicated pure helper allowed). Implement queue, auction identity/actions, duplicate-open path, measurements/condition/photos, cost estimate persistence, provenance editor and comparison. Preserve all dirty-edit, pending and selection guards. Fix evidence date default.
4. Distribution/docs owner after a slot frees: manifests version 0.29.0, README, docs/INSTALL.md, docs/RELEASING.md, docs/RELEASE-0.29.0.md, docs/PRIVACY.md and store publication materials. Reconcile store prep with final behavior; no false signing/account claims, no ID change. Generate listing graphics from verified screenshots. Root owns builds and external account actions.
5. Root baseline and integration verification: all Node tests and Python packaging tests; rebuild once, Firefox lint/install, actual Brave upgrade using retained 0.28 synthetic profile; new workflow, race and backup checks. Reproduce any concern and send fixes back to its Sol owner. Astra design/spec/code/visual review follows integration and fixes.
6. Root release: commit verified work, recheck origin/main and clean installed repo, fast-forward main and push, build canonical Z packages, stage five ZIP assets and concise browser install instructions, verify draft/download digests, publish latest GitHub release and check anonymous downloads. Complete store submission where accounts are available; otherwise deliver prepared assets and identify the exact account step remaining.

Progress lives in ignored .test-artifacts/implementation-0.29.md; each owner writes a separate ignored report. Existing artifacts are retained for upgrade and regression evidence.
