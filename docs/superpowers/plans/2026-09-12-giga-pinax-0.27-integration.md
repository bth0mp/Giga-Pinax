# Giga Pinax 0.27 Integration Implementation Plan

**Execution:** The integration is implemented and Astra-reviewed. Automated and native integration acceptance passed; see [release notes](../../RELEASE-0.27.0.md). The checklists below preserve the original implementation and release procedure.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Combine Giga Pinax 0.26.0 and the auction companion as 0.27.0, delivered to the existing installed folder and origin.

**Architecture:** Retain Giga's research view and session-only price pipeline. Add a separate popup companion controller and the reviewed full-page workspace backed by the companion serialized storage authority. One background registers all menus and routes companion commands without answering Giga's lookup-window messages.

**Tech Stack:** Existing vanilla ES modules/WebExtensions, Node built-in tests, Python standard-library package builder, native Brave QA and Firefox lint.

**Spec:** [Integration design](../specs/2026-09-12-giga-pinax-0.27-integration-design.md).

## Global Constraints

- Implement in `C:/Users/bth0m/CodeWorktrees/giga-pinax-0.27`, branch `codex/companion-0.27`, initially 6aae650. Companion source is the original OneDrive workspace from this task.
- Preserve all Giga lookup/catalogue/price/theme/window behavior and its localStorage keys. No fetched acsearch sales enter persistent companion records.
- Use USD/EUR/GBP/CHF separately with exact minor-unit companion arithmetic. No bid placement, FX, new source hosts, bulk retrieval or service credentials.
- Preserve name Giga Pinax, Firefox ID, shortcut and installed Brave folder; version 0.27.0. User runs no build command.
- Exclusive file ownership below; no simultaneous edits to shared files. Each owner reports meaningful checks and asks Astra for review. Root performs the release commit, final integration and push without force.

## Task 1: Integrate durable backend and package contract — Sol backend

**Files:** New `extension/core/*.js`, `store.js`, `browser-api.js`, `sample-data.js`, `companion-preferences.js`; modify `background.js`, `manifests/*.json`, `scripts/build.py`, `tests/test_packages.py`; bring companion domain/runtime tests and non-colliding fixtures. UI-owned assets are listed in the build allowlist but implemented by Task 2.

**Interfaces:** Preserve `sendCommand(command)`, `getSnapshot()` and `subscribeToSnapshots(listener)` from the companion bridge. Preserve the complete companion command contract. Export `initializeCompanionPreferences(bridge, storage)` to return a validated snapshot reply, migrate only the old companion key if applicable, and initialize only the currency from Giga preferences when absent. UI can use the returned snapshot directly.

- [ ] Add routing regression: send `{type:'giga-pinax-lookup',url:'popup.html?window=1&q=Price+23'}` to the combined background; assert the companion listener returns no response and Giga's real `showInWindow` creates/focuses its window. Assert all three menu IDs coexist after install/startup.
- [ ] Copy reviewed companion core/store/bridge and relevant tests. Extend `CURRENCIES` to include CHF; replace former unsupported-CHF fixtures with genuinely unsupported JPY and add CHF10000 at2500bps => premium2500,total12500; include CHF active exposure and backup round-trip.
- [ ] Implement a single background registrar and explicit command boundary. Keep reminder serialization/recovery unchanged. Initialize companion settings without altering Giga's stored catalogue, recent references, terms, period, currency or theme.
- [ ] Preserve manifest names/IDs/shortcut/hosts; add required companion permissions/assets and 0.27.0. Preserve Giga's Windows file-replacement retry and package safeguards. Package tests assert the union of assets, exact permissions and unchanged Giga identity.
- [ ] Run `node --test tests/*.test.mjs` and `python -m unittest discover -s tests -p 'test_*.py' -v` once Task 2's packaged assets exist. Record earlier focused results separately and send source diff to Astra.

## Task 2: Combine popup and workspace — Sol UI

**Files:** Modify `extension/popup.html`, `popup.css`, `popup.js` only for integration hooks; create `companion-popup.js`, `companion-popup.css`, `workspace.html`, `workspace.css`, `workspace.js`, `current-lot.js`, `source-launchers.js`; port workspace/current-lot/launcher tests and add `tests/companion-popup.test.mjs`. Update README, INSTALL, install landing page and 0.27 release notes. Backend owns manifests/build/sample/preference initializer.

**Interfaces:** Use Task 1's bridge and initializer. Keep Giga controllers independent. A watchlist editor draft uses existing `draft.save` kind `current-lot`, with an explicit whitelisted payload `{target:'watchlist',title,reference,pageUrl}`; do not pass a currentCard object or price outcome. Workspace opens `#lot-draft=<id>`, prefills editable fields and consumes the draft after confirmed successful lot Save. Reviewed research capture stays editable before any query.

- [ ] Test tab click and Arrow/Home/End movement, exact calculator100+25%=125 including CHF/unknown BP, and explicit watchlist draft whitelist. Pass a fixture containing acsearch result rows and prices; assert only allowed reference/link fields appear in the payload.
- [ ] Preserve the whole Giga research subtree and all existing IDs. Add namespaced Calculator/Watchlist panels and controller; no copied conflicting old popup IDs. Preserve auto-focus, keyboard lookup, window updates, theme and announced status. Tab navigation must not initiate source fetches.
- [ ] Add reviewed current-lot capture and Save to watchlist as user actions. Never save source price results, medians or claims. Support fresh reference/selected type input without silently treating a stale displayed card as the newly typed query.
- [ ] Port all companion workspace behaviors and apply Giga theme/tokens. Add CHF to every relevant select/summary. Label saved comparables and the session-only lookup access separately; do not show false blanket “automatic acsearch unavailable” copy. Keep filters, conflicts, reminder defaults, history corrections and validated backups intact.
- [ ] Use the shared initializer on popup/workspace startup. Preserve dirty editor bases and unknown request-ID recovery. Keep source links, draft consumption and collector backups within their intended boundaries.
- [ ] Update installation copy to prebuilt0.27 folders and Reload. Clearly distinguish companion backup data from Giga lookup preferences and explain that another extension origin needs explicit export/import.
- [ ] Run focused tests, inspect the combined popup/workspace with native/browser tooling if available, and request Astra source review. Report package-ready assets to backend.

## Task 3: Astra integration review

- [ ] Review design against retained Giga behavior, all companion features, access/retention boundaries, storage origin, currencies, menu/message dispatch and package identity.
- [ ] Review completed backend and UI diffs; send concrete failing scenarios to the owning Sol agent. Re-review only corrections and relevant interactions until no actionable source finding remains.

## Task 4: Root native verification and release

- [ ] Run the merged JavaScript and Python suites, deterministic build and `npx --yes web-ext@10.6.0 lint --source-dir dist/firefox --warnings-as-errors`. Report any optional development-tool skip accurately.
- [ ] Load the actual built extension in an isolated Brave profile with stubbed ANS/acsearch responses. Verify lookup/catalogue/period controls remain, tabs add no requests,100+25%=125, theme/window/context routes, watchlist-save confirmation, CHF exposure, reminder/badge state, saved evidence/history/backup, and persisted Giga preferences after reload. Do not touch the user's account sessions.
- [ ] Verify no QA data/caches enter package assets, update release notes/ledger with checks and remaining browser limits, and commit owned integration paths.
- [ ] Fetch origin and re-check Z main/status. Resolve new work safely if present; integrate reviewed commit without discarding user edits. Build Z's existing `dist/brave` and `dist/firefox`, verify version/asset identity, then push the release commit to origin without force.
- [ ] Deliver version0.27.0, commit/push status, and the single user step: Reload the existing extension.
