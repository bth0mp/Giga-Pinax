# Giga Pinax 0.28 redesign audit

The 0.28 redesign was checked against the approved product specification on 13 September 2026.

Automated validation passed 379 JavaScript tests and 13 Python tests. Firefox extension lint reported zero warnings, and the packaged extension installed temporarily in Firefox 155.

Native Brave 153 validation confirmed that the explicit research action opens `popup.html?panel=1` in the side panel, retains its query while normal browser tabs change, and leaves the toolbar popup available. A preserved 0.27.2 profile retained its lots, auction events, evidence, collection entries, alternative groups, drafts, and preferences exactly after loading 0.28. Storage remains schema 1 and requires no record migration.

The native Brave workflow also exercised forward and maximum-budget calculator arithmetic, house-premium save/use behavior under a German decimal locale, Settings backup download and import, and workspace edit/save races. Review findings involving stale import previews, async form state, locale input, preference revision races, inactive calculator actions, duplicate premium values, narrow Sources layout, and Settings premium-row overflow were corrected before final validation.

Fetched provider pricing remains session-only. Durable records still pass through the existing revision-checked writer, and old callers preserve optional house-premium presets and lot notes when they omit those fields.

Paid logged-in source sessions, a complete Firefox sidebar UI pass, and a separate Chrome UI pass were not tested.
